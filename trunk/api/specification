Supported MobileBean specification

* A POJO or any Java object that implements the MobileBean interface which serves as a Marker Interface

* Must specify a unique id via a Field Level annotation MobileBeanId

* Supports simple and nested properties

* Supports indexed properties like Arrays and Lists for MobileBeans originating from the server side

* mapped properties are not currently supported on both device and server

* Indexed Properties are not currently supported for beans originating on the device